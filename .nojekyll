# 這個檔存在，GitHub Pages 就會跳過 Jekyll 建置，把所有檔案原樣服務。
#
# 為什麼需要它（2026-09-18 實測）：
#   templates/SKILL.md 有 YAML front matter（--- name / description --- ，
#   那是 skill 格式的必要部分）。Jekyll 看到 front matter 就把它當成要渲染的頁面，
#   轉成 templates/SKILL.html 輸出 —— 原本的 .md 就 404 了。
#
#   症狀長這樣：
#     Pages  templates/SKILL.md   → 404
#     Pages  templates/SKILL.html → 200
#     raw    templates/SKILL.md   → 200   （檔案在 repo 裡好好的）
#
#   其他 27 個連結全部正常，只有這一個被建置系統吃掉，而且沒有任何錯誤訊息。
#   ⇒ 正是 05-guards.md 說的那種「安靜的錯誤」：畫面全對、沒有紅字、
#     只有真的去點才會發現。所以連結要用機器去數，不能用看的。
#
# Jekyll 空檔的慣例是完全空白；這裡放註解是刻意的 —— 訊息要帶著它的來歷。
